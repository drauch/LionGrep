<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="LionGrep.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LionGrep">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Small button used in section headers (e.g. WHAT/FILTER Reset, Settings Add/Remove). -->
            <Style x:Key="SmallButton" TargetType="Button">
                <Setter Property="FontSize" Value="11" />
                <Setter Property="Padding" Value="6,2" />
                <Setter Property="MinWidth" Value="0" />
                <Setter Property="MinHeight" Value="0" />
            </Style>

            <!-- Compact CheckBox: small (~13px) box, 12pt label, no stock min-height padding.
                 Used everywhere we render a dense list of toggles (form, settings, save-preset
                 dialog). Defined at the App level so every window picks it up by key. -->
            <Style x:Key="CompactCheckBox" TargetType="CheckBox">
                <Setter Property="FontSize" Value="12" />
                <Setter Property="MinHeight" Value="0" />
                <Setter Property="Padding" Value="6,0,0,0" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <Grid x:Name="RootGrid" Background="{TemplateBinding Background}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border x:Name="NormalRectangle"
                                        Background="Transparent"
                                        BorderBrush="{ThemeResource ControlStrongStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="3"
                                        Width="13" Height="13"
                                        VerticalAlignment="Center" />
                                <FontIcon x:Name="CheckGlyph" Grid.Column="0"
                                          Glyph="&#xE73E;" FontSize="9"
                                          Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                          Visibility="Collapsed"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center" />
                                <ContentPresenter Grid.Column="1"
                                                  Content="{TemplateBinding Content}"
                                                  Margin="{TemplateBinding Padding}"
                                                  FontSize="{TemplateBinding FontSize}"
                                                  Foreground="{TemplateBinding Foreground}"
                                                  VerticalAlignment="Center" />
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="NormalRectangle.BorderBrush" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed" />
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Opacity" Value="0.4" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                    <VisualStateGroup x:Name="CheckStates">
                                        <VisualState x:Name="Unchecked" />
                                        <VisualState x:Name="Checked">
                                            <VisualState.Setters>
                                                <Setter Target="NormalRectangle.Background" Value="{ThemeResource AccentFillColorDefaultBrush}" />
                                                <Setter Target="NormalRectangle.BorderBrush" Value="{ThemeResource AccentFillColorDefaultBrush}" />
                                                <Setter Target="CheckGlyph.Visibility" Value="Visible" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Indeterminate">
                                            <VisualState.Setters>
                                                <Setter Target="NormalRectangle.Background" Value="{ThemeResource AccentFillColorDefaultBrush}" />
                                                <Setter Target="NormalRectangle.BorderBrush" Value="{ThemeResource AccentFillColorDefaultBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
